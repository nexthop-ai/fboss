# This file contains a list of tests that are excluded from the nhfboss-test.sh
# Each line is a regex pattern that matches one or more test names.
# Keep this file sorted alphabetically rather than always appending to the end
# to reduce merge conflicts. Add a brief comment for each pattern.

# Flaky
AsyncLoggerTest.logTimeoutTest

# Flaky FSDB test
DeltaPublisherTest

# Too many flaky tests
FsdbPubSubManager
FsdbCgoPubSubWrapperTest

# Racy tests
PathPublisherTest

# Flaky tests
RackmonTest.DormantRecovery
StreamClientTest.connectAndCancel
StreamPublisherTest.(overflowQueue|pipeResetWhenServeStreamStuck)
