/sim_agent
/wedge_agent
/external
/external.install
.*.swp
/.build_dir
/.sccache

# Build artifacts
.build/
dist/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/

# Generated at image-build time from bazel-out/volatile-status.txt
/fboss-image/image_builder/templates/centos-09.0/root_files/etc/nexthop_build_info

# Bazel generated files (BUILD.bazel files are auto-generated by bazelify.py)
MODULE.bazel
MODULE.bazel.lock
WORKSPACE.bazel
.bazelrc.sccache
.bazelrc.d
.bazel.d/
bazel-bin
bazel-fboss
bazel-out
bazel-testlogs
# Generated BUILD.bazel files in all subdirectories (but not hand-written ones)
BUILD.bazel
# Hand-written package marker — kept in fboss/build_defs/ only.
!fboss/build_defs/BUILD.bazel
# BGP++ shipit symlink — created at build time by CMakeLists.txt (configure)
# and bazel.sh. Must never be committed: a committed symlink makes Bazel's
# //... globbing follow it into configerator/structs/neteng/fboss/thrift and
# fail target-pattern parsing in the monorepo workspace.
neteng/fboss/bgp/public_tld/configerator/structs/neteng
# Site-specific build configuration (contains infrastructure URLs/credentials)
fboss/oss/scripts/fboss-build.env
